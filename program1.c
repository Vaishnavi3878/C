#inclued <stdio.h>
#inclued <canio.h>
main()
{
	int m1=10, m2=20;//m1 and m2 are local variables.
	clrscr();
	if(m1>m2)
	   printf("/n m1 is greater");
	   else
	   printf("/n m2 is greater");
	getch();
}
